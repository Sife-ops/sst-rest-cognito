{
  "name": "sst-rest-cognito",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.2.11",
    "@serverless-stack/resources": "^1.2.11",
    "@tsconfig/node16": "^1.0.2",
    "@types/node": "^17.0.36",
    "typescript": "^4.7.2",
    "vitest": "^0.13.0"
  },
  "workspaces": [
    "backend"
  ]
}
